# --- Phase 1 (2D solver, pure NumPy on CPU) ---
numpy>=1.24
matplotlib>=3.7      # 2D field/profile visualization
pyevtk>=1.6.0        # VTK export for ParaView
pyyaml>=6.0          # run configuration files
pytest>=7.4          # tests

# --- Phase 2+ (3D, GPU) ---
# Taichi needs Python <=3.12 (no 3.14 wheel). Use a dedicated 3.12 venv:
#   py -3.12 -m venv .venv  &&  .venv\Scripts\activate  &&  pip install -r requirements.txt
taichi>=1.7.0        # LBM kernels, CUDA backend on the RTX 3090

